# Server (Railway) production environment variables
# Copy to Railway project variables

# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host:5432/url_shortener?sslmode=require

# Strong JWT secret (32+ random bytes)
JWT_SECRET=replace_with_strong_random_secret

# CORS: allow your Vercel frontend origin
CORS_ORIGIN=https://your-frontend.vercel.app

# Optional: Port provided by Railway
PORT=8080

# Optional: Node environment
NODE_ENV=production
