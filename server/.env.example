# Server environment variables (example)
# Copy to .env and fill with your values. Do NOT commit real secrets.

# App
PORT=5001
NODE_ENV=development

# Database
DATABASE_URL=postgresql://USER@localhost:5432/url_shortener

# Auth
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace_with_strong_random_secret

# CORS
FRONTEND_URL=http://localhost:3000

# Public base URL used to build short links
# In production, set to your Railway domain (e.g., https://your-api.railway.app)
BASE_URL=http://localhost:5001

# Production settings:
# NODE_ENV=production
# FRONTEND_URL=https://your-frontend-domain.vercel.app
# BASE_URL=https://your-backend-domain.railway.app
