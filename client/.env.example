# Client environment variables (example)
# Copy to .env and fill with your values. Do NOT commit real secrets.

# API base URL used by axios client
# In production, set to your Railway backend URL
VITE_API_URL=http://localhost:5001

# Optional: base URL used when rendering short links in UI
VITE_BASE_URL=http://localhost:5001
